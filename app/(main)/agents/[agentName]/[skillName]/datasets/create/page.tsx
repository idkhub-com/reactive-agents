'use client';

import { CreateDatasetView } from '@client/components/agents/skills/datasets';
import type { ReactElement } from 'react';

export default function CreateDatasetPage(): ReactElement {
  return <CreateDatasetView />;
}
