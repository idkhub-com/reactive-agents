'use client';

import { DatasetsView } from '@client/components/agents/skills/datasets';
import type { ReactElement } from 'react';

export default function DatasetsPage(): ReactElement {
  return <DatasetsView />;
}
