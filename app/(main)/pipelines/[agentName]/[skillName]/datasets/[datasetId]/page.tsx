'use client';

import { DatasetDetailView } from '@client/components/pipelines';
import type { ReactElement } from 'react';

export default function DatasetDetailPage(): ReactElement {
  return <DatasetDetailView />;
}
