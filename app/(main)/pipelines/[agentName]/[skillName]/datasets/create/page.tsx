'use client';

import { CreateDatasetView } from '@client/components/pipelines';
import type { ReactElement } from 'react';

export default function CreateDatasetPage(): ReactElement {
  return <CreateDatasetView />;
}
