'use client';

import { DatasetsView } from '@client/components/pipelines';
import type { ReactElement } from 'react';

export default function DatasetsPage(): ReactElement {
  return <DatasetsView />;
}
